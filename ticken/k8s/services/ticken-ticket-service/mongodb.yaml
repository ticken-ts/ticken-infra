apiVersion: apps/v1
kind: Deployment
metadata:
  name: ticken-ticket-service-mongodb
spec:
  replicas: 1

  selector:
    matchLabels:
      app: ticken-ticket-service-mongodb

  template:
    metadata:
      labels:
        app: ticken-ticket-service-mongodb

    spec:
      volumes:
        - name: ticken-ticket-service-mongodb-volume
          persistentVolumeClaim:
            claimName: common-pvc

      containers:
        - name: ticken-ticket-service-mongodb
          image: ${MONGODB_IMAGE}
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              value: "admin"
            - name: MONGO_INITDB_ROOT_PASSWORD
              value: "admin"

          volumeMounts:
            - name: ticken-ticket-service-mongodb-volume
              mountPath: /data/db
              subPath: ticken-ticket-service/db
---
apiVersion: v1
kind: Service
metadata:
  name: ticket-service-mongodb-clusterip

spec:
  type: ClusterIP
  selector:
    app: ticken-ticket-service-mongodb

  ports:
    - name: ticken-ticket-service-mongodb
      protocol: TCP
      port: 27017
      targetPort: 27017